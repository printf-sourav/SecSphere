# API base URL used by the frontend in production or non-proxy environments.
# Keep /api for same-origin deployments where backend serves frontend.
VITE_API_BASE_URL=/api

# Optional: override local Vite dev proxy target.
# Example: http://localhost:5000
VITE_DEV_API_PROXY_TARGET=http://localhost:5000
